<% @user ||= current_user %>
<%= form_for(@user,url:notice_follow_path, remote: true) do |f| %>
  <div><%= hidden_field_tag :id, @user.id %></div>
  <%= f.submit "notice when followed", class: "btn btn-primary" %>
<% end %>