<% @user ||= current_user %>
<div class="search_form col-md-7 col-md-offset-1">
  <%= form_tag(search_microposts_path, method: :post, remote: true, class:"form-inline") do |f| %>
    <%= search_field_tag("search",nil, class: "form-control col-md-10") %>
    <%= submit_tag("検索", class: "btn btn-primary col-md-2") %>
  <% end %>
</div>
