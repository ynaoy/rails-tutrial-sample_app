<div class="users_wrapper">

  <%= will_paginate @users, params: { search: params[:search] }%>
  <ul class="users">
    <% @users.each do |user| %>
      <%= render @users %>
      <% end %>
    </ul>

    <%= will_paginate @users, params: { search: params[:search] } %>

</div>

